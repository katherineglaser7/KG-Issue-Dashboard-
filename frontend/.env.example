# Frontend Environment Variables

# Backend API URL (defaults to http://localhost:8000 if not set)
VITE_API_URL=http://localhost:8000

# Default repository to load on startup (format: owner/repo)
# Leave empty to require user to enter a repo manually
VITE_DEFAULT_REPO=
